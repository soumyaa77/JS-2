const name="soumya"
const repoCount=50
// od way to concat == console.log(name+repoCount+"Value");
//back ticks `` are the new way to write 
console.log(`Hello my name is ${name}`);
//benefit is : string interpolation, we can add methods inside curly braces
const gameName=new String("hitesh"); //object
console.log(gameName[0]);

